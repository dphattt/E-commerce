/** Default icon hit target — 28×28px (`size-store-icon`). */
export const ICON_BLOCK_PX = 28;

export const iconBlockClassName =
  "inline-flex size-store-icon shrink-0 items-center justify-center";

/** Glyph size inside a 28×28 icon block. */
export const iconGlyphClassName = "size-4";

/** Payment method badge — 42×25px. */
export const PAYMENT_BADGE_WIDTH_PX = 42;
export const PAYMENT_BADGE_HEIGHT_PX = 25;

export const paymentBadgeClassName =
  "inline-flex h-store-payment w-store-payment shrink-0 items-center justify-center overflow-hidden rounded px-1";
